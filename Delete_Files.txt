1.在版本库中删除文件：
					$ rm <文件名>                                       #在文件资源管理器
2.删除版本库文件后工作区和版本库就不一致：
这时，你有两个选择：
一是确实要从版本库中删除该文件，那就用命令git rm删掉，并且git commit：
另一种情况是删错了，因为版本库里还有呢，所以可以很轻松地把误删的文件恢复到最新版本：
3.使用命令恢复：
				git checkout -- <文件名>                                #恢复文件到最新版本，会丢失最近一次提交后你修改的内容